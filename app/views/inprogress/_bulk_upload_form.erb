<%= nested_form_for(@potential_perspective, :url => {:action=>"create", :controller=>"inprogress"}, :method => "post", :html => {:multipart=> true}) do |f|%>
  <% if @potential_perspective.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@potential_perspective.errors.count, "error") %> <%= t'inprogress.error' %></h2>

      <ul>
      <% @potential_perspective.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div id="csv_file">
      <p class="instruction"><%= t'inprogress.csv_file' %></p>
      <p><%= file_field_tag :upload %></p>
    </div>
  </div>
  <div class="actions">
    <%= f.submit :class => 'button', :value => 'Get Going' %>   
  </div>
<% end %>
