<tr style="padding: 0px;">
  <td width="100%" colspan="3" class="table_style">
    <p class="emphasize embiggen" style="margin: 5px;margin-left:0px;margin-top:25px;margin-bottom:0px;padding: 0px;font-weight: bold;font-size: 18px;color:#357A80"><%= t('weekly.explore') %></p>
  </td>
</tr>

<tr style="padding: 0px;">
  <!-- Pictures -->
  <% recos['places'].each do |place| %>
    <td valign="top" width="220" class="table_style" colspan="1">
      <div class="email_guide_holder" style="height:114px;">
        <div class="email_venue_pic_holder">
          <a href="<%= url_for(:controller => "places", :action => "show", :id => place.slug, :host => ActionMailer::Base.default_url_options[:host]) %>">
            <% if place.pictures.length > 0 %>
                <img src="<%= place.pictures[0].mosaic_3_2_url %>" style="max-height: 98px;max-width: 210px;">
            <% else %>
                <img src="https://maps.google.com/maps/api/staticmap?center=<%= place.loc[0] %>,<%= place.loc[1] %>&zoom=14&size=210x98&&markers=icon:http://www.placeling.com/images/marker.png%%7Ccolor:red%%7C<%= place.loc[0] %>,<%= place.loc[1] %>&sensor=false">
            <% end %>
          </a>
        </div>
      </div>
    </td>
  <% end %>
  <% places_filler.each do |place| %>
    <td width="220" class="table_style" colspan="1">
      <div class="email_empty_guide_holder">&nbsp;</div>
    </td>
  <% end %>
</tr>

<!-- Names -->
<% recos['places'].each do |place| %>
    <td valign="top" width="220" class="table_style">
      <div style="margin: 0px;padding: 0px;text-align:center">
        <div class="username_email" style="margin:5px;<% if @pictures == false %>margin-left:0px;<% end %>padding: 0px;width:210px;">
          <%= link_to place.name, vanity_track_url_for(Vanity.context.vanity_identity, :click_place, :controller => "places", :action => "show", :id => place.slug, :host => ActionMailer::Base.default_url_options[:host]), :class => "placename_link" %>
        </div>
      </div>
    </td>
<% end %>
<% places_filler.each do |place| %>
  <td width="220" class="table_style" colspan="1">
    <div class="email_empty_guide_holder">&nbsp;</div>
  </td>
<% end %>