<h1>Listing publisher_categories</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @publisher_categories.each do |publisher_category| %>
      <tr>
        <td><%= link_to 'Show', publisher_category %></td>
        <td><%= link_to 'Edit', edit_publisher_category_path(publisher_category) %></td>
        <td><%= link_to 'Destroy', publisher_category, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'New Publisher category', new_publisher_category_path %>
