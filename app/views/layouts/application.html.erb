<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8"/>
  <meta property="og:site_name" content="Placeling" />
  <meta property="fb:app_id" content="280758755284342" />

  <%= yield :head %>
  <%= favicon_link_tag %>
  <%= csrf_meta_tag %>

    <!-- Social widgets -->
    <script charset="utf-8" src="//platform.twitter.com/widgets.js" type="text/javascript"></script>

    <!-- jQuery thats self.element.propAttr is not a functionloaded before document ready to prevent flickering - Rest are found at the bottom -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

    <%= javascript_include_tag "externals" %>
    <%= javascript_include_tag "application" %>

  <%= yield :javascript_includes %>

  <%= stylesheet_link_tag "application" %>
  <style type="text/css">
      .ui-autocomplete {
          font-size: 62.5%;
      }
    <%= yield :css %>
  </style>
</head>
<body>
    <input type="hidden" id="default_lat" name="default_lat" value="<%= @default_lat %>" />
    <input type="hidden" id="default_lng" name="default_lng" value="<%= @default_lng %>" />
  <!--[if lte IE 7]><script src="javascripts/ie6/warning.js"></script><script>window.onload=function(){e("javascripts/ie6/")}</script><![endif]-->
  <div id="container">
    <div id="wrapper">
      <%= render :partial => 'layouts/header' %>
      <div id="content">
        <% if notice %>
          <div id="notice" class="flash">
            <%= content_tag :div, notice unless notice.blank? %>
          </div>
        <% end %>
        <% if alert %>
          <div id="alert" class="flash">
            <%= content_tag :div, alert unless alert.blank? %>
          </div>
        <% end %>
        <div id="maincontent">
            <%= yield %>
        </div>
      </div>
    </div>
  </div>
  <%= render :partial => 'layouts/footer' %>
     <script type="text/javascript">

       var default_lat = $("#default_lat").val();
       var default_lng = $("#default_lng").val();

      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-25331124-1']);
      _gaq.push(['_setDomainName', 'placeling.com']);
      _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();

    </script>
    <div id="fb-root"></div>
    <script>(function(d, s, id) {
      var js, fjs = d.getElementsByTagName(s)[0];
      if (d.getElementById(id)) return;
      js = d.createElement(s); js.id = id;
      js.src = "//connect.facebook.net/en_US/all.js#xfbml=1";
      fjs.parentNode.insertBefore(js, fjs);
    }(document, 'script', 'facebook-jssdk'));
    </script>
  <%= yield :javascript %>
</body>
</html>
